# Flow Combat Prototype — CLAUDE.md

## プロジェクト概要

中世フローチャート戦闘システムのHTMLプロトタイプ。
ローグライト・デッキ構築・陣地取りストラテジーゲームの戦闘部分を検証するためのもの。

詳細な設計仕様は `GDD_v1.md` を参照すること。

## 現在のフェーズ

**ビジュアル改修フェーズ**（2025年〜）

戦闘システムの大枠は確定済み。現在の目標は「ゲームとして面白いかどうかを感覚で判断できる状態にする」こと。
具体的には以下の3点を順次実装する：

1. 戦闘フィールドのユニット表示をアイコン・グラフィック付きに変える
2. 攻撃時のエフェクト（ダメージ数字フロート、フラッシュ等）を追加する
3. 背景・雰囲気を中世戦場らしくする

## ファイル構成と役割

```
Flow_Combat_Prototype/
├── flowchart_proto_v57.html   メインHTML（UIの骨格）
├── GDD_v1.md                  ゲームデザインドキュメント（仕様の正）
├── scripts/
│   ├── data.js                ユニット定義・ゲームデータ
│   ├── enemies.js             敵編成・敵ユニット定義
│   ├── enemy_actions.js       敵行動パターン定義
│   ├── action_handlers.js     敵アクションの実装（atk/def/volley等）
│   ├── controls.js            ユーザー操作・フロー編集
│   ├── render.js              描画・DOM更新（renderMyUnits, renderEnemies等）
│   ├── battle_ui.js           戦闘中のUI・一時停止・結果表示
│   ├── battle_passive.js      パッシブスキル定義と発動
│   ├── battle_action.js       プレイヤーユニットのアクション実行
│   └── battle_logic.js        戦闘ループのメインロジック
└── styles/
    └── prototype.css          スタイル定義
```

## 変更ルール

### 触っていいファイル（ビジュアル改修対象）
- `render.js` — ユニット表示・DOM生成を変えるならここ
- `battle_ui.js` — エフェクト・アニメーション追加はここと連動
- `prototype.css` — スタイル追加・変更
- `flowchart_proto_v57.html` — HTML構造の変更が必要な場合のみ

### 触ってはいけないファイル（システムロジック）
- `battle_logic.js` — 戦闘ループの根幹。絶対に変えない
- `battle_action.js` — アクション実行ロジック。変えない
- `battle_passive.js` — パッシブ発動ロジック。変えない
- `action_handlers.js` — アクション定義。変えない
- `data.js` — ユニットデータ。変えない
- `enemies.js` — 敵定義。変えない
- `enemy_actions.js` — 敵行動パターン。変えない

### 例外
GDDの仕様変更に伴うデータ修正が必要な場合はその都度確認する。

## ビジュアル方針

- 世界観：中世近世・リアル寄りのフィクション（史実根拠あり）
- 雰囲気：重厚・戦場感・テンションが上がる暗めのトーン
- アニメーション：攻撃の因果関係が視覚的にわかることを優先
- グラフィック：フリー素材またはSVGアイコンを使用（ドット絵歓迎）
- 「業務アプリ感」を払拭することが最優先

## ゲームシステムの要点（指示に必要な最低限）

- プレイヤーはユニットをフローチャートに並べ、START→ENDの順に実行される
- 補給（Supply）がフローの燃料。補給が尽きるとループが止まる
- 出力種別：ATK（蓄積型）・DEF（シールド）・即時ダメージ・士気・補給
- 弓兵は残弾制限あり・弩兵は装填→発射の2ステート制
- 敵はターンごとに行動パターンを実行（enemy_actions.jsで定義）
- 戦闘は最大10ターン・10ループ

## 将来の移植先

最終的にはGodot 4に移植する。
HTMLプロトタイプはあくまでシステム・ビジュアルの検証用。
Godot移植前に「これは面白い」という感触を得ることが現フェーズの目標。
